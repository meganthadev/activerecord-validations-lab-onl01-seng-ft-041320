class TitleValidator < ActiveModel::Validator

  def validate(record)
    unless record.title.match?("Won't Believe", "Secret", "Top [number]", or "Guess")
      record.errors[:title] << "We need clickbait!"
    end
  end
  
end 